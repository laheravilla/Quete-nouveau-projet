{% extends 'base.html.twig' %}

{% block title %}All articles{% endblock %}

{% block body %}
    <h1 class="mt-5">All articles from article's table:</h1>

    <table class="table mt-5">
        <thead>
        <tr>
            <th>#</th>
            <th>Title</th>
            <th>Category</th>
            <th>Content</th>
            <th>More...</th>
        </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ article.title }}</td>
                <td>{{ article.category.name }}</td>
                <td>{{ article.content }}</td>
                <td>
                    <a class="btn btn-outline-primary" href="{{ path('blog_show', { 'title': article.title }) }}">Read more... </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Any article found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_index') }}">Back to Homepage</a>
{% endblock %}
